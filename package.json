{
  "name": "botland-gallery",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "materialize": "node scripts/assemble-source.mjs",
    "predev": "npm run materialize",
    "dev": "vite",
    "precheck": "npm run materialize",
    "check": "tsc --noEmit && node --experimental-strip-types scripts/validate-catalog.ts",
    "build": "npm run check && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.2.1"
  }
}
